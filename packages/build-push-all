#!/usr/bin/perl

use warnings;
use strict;

my @versions=(`ghcup list -r -o | grep -Po '^ghc\\s[\\d.]+' | sed 's/ghc\\s*//g'`);

for (@versions) {

  chop $_;

  system("HASKELL_GHC_VERSION=$_ ./build-push");

}
